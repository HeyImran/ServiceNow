function onSubmit() {
var phone =   g_form.getValue('description');    
var regex = /^\d{10}$/; 
if (!regex.test(phone)) { 
g_form.showFieldMsg('description', 'Enter a 10-digit number', 'error'); 
return false; 
} 
return true; 
   
}
